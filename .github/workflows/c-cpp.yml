name: TiCS

on: pull_request

jobs:
  TiCS:
    name: TiCS
    runs-on: janus
    steps:
    - name: Checkout
      uses: actions/checkout@v3    
    - name: TICS Action Run
      uses: tiobe/tics-github-action@33235-better_retry_logging
      with:
        projectname: demo
        clientData: 'try'
        installTics: true
        calc: LOC,CS
        #filelist: filelist.txt
        ticsConfiguration: 'http://192.168.1.11:80/tiobeweb/TICS/api/cfg?name=default'
        githubToken: ${{secrets.GITHUB_TOKEN}}
        postAnnotations: true
        postToConversation: true
        # hostnameVerification: 0
        # trustStrategy: self-signed
        tmpDir: /home/tester/tmps/33235-before
        # additionalFlags: -log 1000000
